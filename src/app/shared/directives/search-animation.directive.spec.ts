import { SearchAnimationDirective } from './search-animation.directive';

describe('SearchAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
