import { InputDisabledDirective } from './input-disabled.directive';

describe('InputDisabledDirective', () => {
  it('should create an instance', () => {
    const directive = new InputDisabledDirective();
    expect(directive).toBeTruthy();
  });
});
